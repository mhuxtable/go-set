language: go
go:
  - 1.12
  - 1.13
  - 1.14
  - stable
  - tip

env:
  - GO111MODULE=on

before_install:
  - go get golang.org/x/tools/cmd/cover
  - go get github.com/mattn/goveralls

script:
  - go test -v -covermode=count -coverprofile=coverage.out ./...
  - $HOME/gopath/bin/goveralls -coverprofile=coverage.out -service=travis-ci -repotoken $COVERALLS_TOKEN

  - [[ "$(go version") =~ "1.12" ]] || cd genset && go test -v -covermode=count -coverprofile=coverage.out ./...
  - [[ "$(go version") =~ "1.12" ]] || $HOME/gopath/bin/goveralls -coverprofile=coverage.out -service=travis-ci -repotoken $COVERALLS_TOKEN
